const nextConfig = {
  output: 'export',
  distDir: 'dist',
  basePath: '/ai-news-digest',
  assetPrefix: '/ai-news-digest/',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
};

module.exports = nextConfig;
